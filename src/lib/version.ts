// App version. Convention: MAJOR.MINOR.PATCH where PATCH (3 digits, zero-padded)
// is bumped for small fixes, MINOR for bigger features, MAJOR for breaking changes.
// Bump this on every release so users can confirm they're on the latest build.
export const APP_VERSION = "1.0.001";
export const APP_VERSION_LABEL = `v${APP_VERSION}`;
